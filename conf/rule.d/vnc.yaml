# 目前不支持arm架构的windows和linux操作系统
- name: vnc            # 链路名称
  target: remote       # 目标客户端ID
  type: vnc            # web vnc
  local_addr: 0.0.0.0  # 示例监听所有网卡；无独立登录认证，建议改为127.0.0.1
  # local_port: 5900     # 本地监听端口号
  fps: 10              # 刷新频率
